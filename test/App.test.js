/** @jest-environment node */

'use strict';

// const App = require('../src/App.jsx');

describe('App root component', () => {
  it('placeholder to pass', () => {
    expect(2 + 3).toEqual(5);
  });

  // it('renders correctly, () => {
  //   const wrapper = document.createElement('div');
  //   const component =
  // });
});
